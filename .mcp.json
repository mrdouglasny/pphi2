{
  "mcpServers": {
    "gemini": {
      "command": "uv",
      "args": ["run", "--python", "3.10", "--with", "mcp", "--with", "google-genai", "--with", "python-dotenv", "python", "../gaussian-field/tools/gemini_mcp/server.py"]
    },
    "lean-lsp": {
      "command": "uvx",
      "args": ["lean-lsp-mcp"]
    }
  }
}
