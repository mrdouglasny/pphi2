# `UniformExponentialMoment.lean` — Informal Summary

> **Source**: [`Pphi2/IRLimit/UniformExponentialMoment.lean`](../../../Pphi2/IRLimit/UniformExponentialMoment.lean)
>
> **Generated**: 2026-03-20; manually refreshed for Green-moment refactor on 2026-05-07
>
> **Note**: Auto-generated by `/lean-summarize`. Re-run to refresh.

## Overview

Provides the uniform-in-$L_t$ exponential moment bound $\mathbb{E}_{\nu_{L_t}}[\exp(|\omega(f)|)] \le K \cdot \exp(C\, q(f)^2)$ needed for OS0 analyticity of the IR limit. This is pulled through from the asymmetric torus Nelson/Frohlich bound via the cylinder-to-torus embedding and the method-of-images Green function comparison. Together with bounded-continuous convergence of the extracted limit, the bound supplies the domination used in the proved integral analyticity argument.

## Status
**Main result**: 0 axioms, 0 sorry(s)
`cylinderIR_uniform_exponential_moment` and `cylinderIR_uniform_second_moment`
are theorems conditional on the explicit `MeasureHasGreenMomentBound` input.

---

### `cylinderIR_uniform_exponential_moment` (theorem)

```lean
theorem cylinderIR_uniform_exponential_moment
    (mass : ℝ) (hmass : 0 < mass)
    (KG CG : ℝ) (hKG_pos : 0 < KG) (hCG_pos : 0 < CG) :
    ∃ (K C : ℝ) (q : Seminorm ℝ (CylinderTestFunction Ls)),
    0 < K ∧ 0 < C ∧ Continuous q ∧
    ∀ (Lt : ℝ) [Fact (0 < Lt)] (_ : 1 ≤ Lt)
      (μ : Measure (Configuration (AsymTorusTestFunction Lt Ls)))
      [IsProbabilityMeasure μ]
      (_ : MeasureHasGreenMomentBound Ls mass hmass KG CG μ)
      (f : CylinderTestFunction Ls),
    Integrable (fun ω : Configuration (CylinderTestFunction Ls) =>
      Real.exp (|ω f|)) (cylinderPullbackMeasure Lt Ls μ) ∧
    ∫ ω : Configuration (CylinderTestFunction Ls),
      Real.exp (|ω f|) ∂(cylinderPullbackMeasure Lt Ls μ) ≤
    K * Real.exp (C * q f ^ 2)
```

**Informal statement**: There exist constants $K, C > 0$ and a continuous seminorm $q$ such that for all $L_t \ge 1$ and all torus measures $\mu$ satisfying the Green-controlled moment bound with uniform constants, the cylinder pullback exponential moment is integrable and bounded:
$$\int \exp(|\omega(f)|)\, d\nu_{L_t} \le K \cdot \exp\bigl(C\, q(f)^2\bigr)$$
uniformly in $L_t$.

**Proof**: Compose `MeasureHasGreenMomentBound` with `cylinderPullback_expMoment_uniform_bound` and the method-of-images estimate `torusGreen_uniform_bound`. The abstract `AsymSatisfiesTorusOS.os1` clause is deliberately not used because its seminorm is not uniform in `Lt`.

---
*This file has **0** axioms and **0** sorries.*
